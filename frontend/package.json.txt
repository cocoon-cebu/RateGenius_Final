{
  "name": "rategenius-frontend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "chmod +x ./node_modules/.bin/vite && vite build",
    "dev": "vite",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
